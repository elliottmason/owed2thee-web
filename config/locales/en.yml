en:
  errors:
    format: "%{attribute} %{message}"
    messages:
      identical_users: you cannot create a %{record} with yourself
      nonpositive_amount: amount must be greater than $0.00
  controllers:
    application:
      cancel:
        flash:
          notice: 'cancellation completed'
      confirm:
        flash:
          notice: 'confirmation successful'
      dispute:
        flash:
          notice: 'dispute submitted'

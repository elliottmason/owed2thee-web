en:
  activerecord:
    errors:
      models:
        loan:
          attributes:
            borrower:
              exclusion: 'You cannot borrow from yourself.'
            lender:
              exclusion: 'You cannot lend to yourself.'
  errors:
    format: "%{attribute} %{message}"
    messages:
      identical_users: you cannot create a %{record} with yourself
      nonpositive_amount: amount must be greater than $0.00
  controllers:
    application:
      cancel:
        flash:
          notice: "%{cancelable} has been canceled."
      confirm:
        email_address: "Your email address %{email_address}"
        flash:
          notice: "%{confirmable} is now confirmed."
      dispute:
        flash:
          notice: "Dispute againist %{disputable} has been submitted."
        loan:     "%{creator}'s loan for %{amount}"
        payment:  "%{creator}'s payment of %{amount}"

# Elliott M. marked his loan to you as fully paid
# You sent $10.00 to Elliott M. using 0.03448BTC
# You confirmed: Elliott M. lent you $10.00
# Elliott M. commented on his $10.00 loan to you
# You commented on your $10.00 loan from Elliott
# You disputed: Elliott M. lent you $10.00
# You submitted a loan: lent Josh $10.00
# Elliott M. submitted a loan: lent Josh $10.00
# Elliott M. submitted a loan: lent you $10.00

en:
  app:
    adjectives:
      canceled: canceled
      confirmed: confirmed
      disputed: disputed
      paid: paid
      submitted: submitted
    default_display_name:
      string: 'an %{title} %{user}'
      title: :app.title
      user: :app.nouns.user
    notices:
      cancellation: "canceled %{cancellable}"
      confirmation: "confirmed %{confirmable}"
      dispute:
        dispute: :app.nouns.dispute
        string: "%{dispute} against %{disputable} has been submitted"
    nouns:
      cancellation: cancellation
      confirmation: confirmation
      debt: debt
      dispute: dispute
      loan: loan
      payment: payment
      user: user
    verbs:
      cancel: cancel
      confirm: confirm
      dispute: dispute
      pay: pay
      submit: submit
    title: Owed2Thee

  email_addresses:
    notices:
      confirmation:
        confirmable: "your email address %{email_address}"
        string: :app.notices.confirmation

  loans:
    descriptions:
      activity:
        loan: :loans.name
        created: "%{creator} submitted a %{loan}: lent %{borrower} %{amount_lent}"
      short:
        as_borrower:
          loan: :loans.name
          string: "%{lender}'s %{loan} for %{amount_lent}"
        as_lender:
          loan: :loans.name
          string: "your %{loan} to %{borrower} for %{amount_lent}"
    errors:
      amount:
        nonpositive: :transfers.errors.amount.nonpositive
      identical_users:
        record: :loans.name
        string: :transfers.errors.identical_users
    name: :app.nouns.loan
    notices:
      cancellation:
        cancellable: :loans.descriptions.short.as_lender
        string: :app.notices.cancellation
      confirmation:
        as_borrower:
          confirmable: :as_borrower
          string: :app.notices.confirmation
        as_lender:
          confirmable: :as_lender
          string: :app.notices.confimration
      dispute:
        as_borrower:
          disputable: :loans.descriptions.short.as_borrower
          string: :app.notices.dispute
        as_lender:
          disputable: :loans.descriptions.short.as_lender
          string: :app.notices.dispute
    # Rails scopes
    show:
      cancel_button: :app.verbs.cancel
      confirm_button: :app.verbs.confirm
      dispute_button: :app.verbs.dispute
      pay_button: :app.verbs.pay

  passwords:
    errors:
      current_password:
        incorrect: is incorrect.

  payments:
    errors:
      amount:
        nonpositive: :transfers.errors.amount.nonpositive
    name: :app.nouns.payment
    notices:
      confirmation:
        as_payee:
          confirmable: "a %{amount_paid} %{payment} from %{payer}"
          payment: :payments.name
          string: :app.notices.confirmation
        as_payer:
          confirmable: "your %{amount_paid} %{payment} toward %{payable}"
          payable: :loans.descriptions.short.as_borrower
          payment: :payments.name
          string: :app.notices.confirmation

  transfers:
    errors:
      amount:
        nonpositive: must be larger than $0.00
      identical_users: "you cannot create a %{record} with yourself"

  random_phrases:
    negative:
      - dang it
      - error
      - hold up,
      - ["I don't think so, Tim.", 0.0078125]
      - oops
      - that's no good
      - uh oh
    neutral:
      - done
      - finished
      - OK
      - [verily, 0.0625]
    positive:
      - delightful
      - done
      - fantastic
      - [five by five, 0.5]
      - got it
      - great
      - it is so
      - [marvelous, 0.1]
      - perfect
      - splendid
      - [splendiferous, 0.0078125]
      - ["you got it, dude", 0.25]

  # scoped for Rails
  errors:
    attributes:
      amount:
        greater_than: :transfers.errors.amount.nonpositive

  # scoped for PublicActivity
  activity:
    loan:
      created: '%{creator} submitted a loan: lent %{borrowers} %{amount_lent}'
